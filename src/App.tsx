import {useMemo,useState} from "react";import {Search,ExternalLink,Sparkles} from "lucide-react";import {tools} from "@/data/tools";import {categories} from "@/data/categories";
export default function App(){const[q,setQ]=useState("");const[cat,setCat]=useState("");const filtered=useMemo(()=>tools.filter(t=>(!q||`${t.name} ${t.description} ${t.categories.join(" ")}`.toLowerCase().includes(q.toLowerCase()))&&(!cat||t.categories.includes(cat))),[q,cat]);return <div className="min-h-screen"><header className="sticky top-0 z-10 border-b border-line bg-porcelain/95 backdrop-blur dark:border-line-dark dark:bg-ink/95"><div className="mx-auto flex max-w-7xl items-center justify-between gap-4 px-4 py-4"><a href="/" className="font-display text-xl font-semibold">Anjal AI</a><span className="hidden text-sm text-slate sm:block">Discover AI tools</span></div></header><main className="mx-auto max-w-7xl px-4 py-10"><section className="mx-auto max-w-3xl text-center"><span className="inline-flex items-center gap-2 rounded-full border border-line bg-white px-3 py-1 text-xs font-medium text-teal-700 dark:border-line-dark dark:bg-surface-dark"><Sparkles className="h-3.5 w-3.5"/>500 AI tools indexed</span><h1 className="mt-5 text-4xl font-semibold tracking-tight sm:text-6xl">Find the right AI tool.</h1><p className="mt-4 text-slate">Search, filter and explore AI tools by category. Official links are provided; unverified commercial fields are not fabricated.</p><div className="mt-8 flex items-center gap-2 rounded-xl border border-line bg-white p-2 shadow-card dark:border-line-dark dark:bg-surface-dark"><Search className="ml-2 h-5 w-5 text-slate"/><input className="w-full bg-transparent px-2 py-3 outline-none" placeholder="Search AI tools..." value={q} onChange={e=>setQ(e.target.value)}/></div></section><div className="mt-10 flex flex-wrap gap-2"><button onClick={()=>setCat("")} className={`rounded-full border px-3 py-1.5 text-xs ${!cat?"border-teal bg-teal-50 text-teal-700":"border-line"}`}>All</button>{categories.map(c=><button key={c.slug} onClick={()=>setCat(c.slug)} className={`rounded-full border px-3 py-1.5 text-xs ${cat===c.slug?"border-teal bg-teal-50 text-teal-700":"border-line"}`}>{c.name}</button>)}</div><div className="mt-8 flex items-center justify-between"><p className="text-sm text-slate">{filtered.length} tools</p></div><div className="mt-4 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{filtered.map(t=><article key={t.id} className="rounded-card border border-line bg-white p-5 shadow-card transition hover:-translate-y-0.5 dark:border-line-dark dark:bg-surface-dark"><div className="flex items-start justify-between gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-lg bg-teal-50 text-sm font-bold text-teal-700">{t.logo}</span><a href={t.website} target="_blank" rel="noreferrer" className="text-slate hover:text-teal"><ExternalLink className="h-4 w-4"/></a></div><h2 className="mt-4 text-lg font-semibold">{t.name}</h2><p className="mt-2 text-sm text-slate">{t.description}</p><div className="mt-4 flex items-center justify-between"><span className="rounded-full bg-line/60 px-2 py-1 text-[11px]">{t.categories[0]}</span><span className="text-[11px] text-slate">Official link</span></div></article>)}</div></main><footer className="mt-16 border-t border-line px-4 py-8 text-center text-xs text-slate dark:border-line-dark">© 2026 Anjal AI Directory · Built for discovery</footer></div>}
