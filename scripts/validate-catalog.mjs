import fs from "node:fs";const files=fs.readdirSync("src/data/catalog").filter(x=>x.endsWith(".ts"));const ids=files.flatMap(f=>(fs.readFileSync(`src/data/catalog/${f}`,"utf8").match(/"id":"[^"]+"/g)||[]));const unique=new Set(ids);if(ids.length!==500||unique.size!==500)throw new Error(`Expected 500 unique catalog tools; found ${ids.length}`);console.log(`Catalog entries: ${ids.length}`);console.log("Catalog validation passed.");
